[env]
TERM = "xterm-256color"

# [shell]
# program = "zsh"

[window]
padding.x = 10
padding.y = 10
decorations = "Buttonless"
opacity = 0.98

# [keyboard]
# bindings = [{ key = "F11", action = "ToggleFullscreen" }]
