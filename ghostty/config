# Import theme (switched by apply_theme.sh)
config-file = ?theme.conf

# Import font (switched by apply_font.sh)
config-file = ?font.conf

# Import font size (switched by apply_font_size.sh)
config-file = ?font-size.conf

# Window
window-padding-x = 10
window-padding-y = 10
window-decoration = false
confirm-close-surface = false
macos-titlebar-style = hidden

# Cursor
cursor-style = block
cursor-style-blink = false
shell-integration-features = no-cursor

# Background opacity
background-opacity = 0.98

# Shell - no login flag since we immediately exec into tmux anyway
command = /opt/homebrew/bin/bash

# Quit Ghostty when last window closes
quit-after-last-window-closed = true
