[env]
TERM = "xterm-256color"

# [shell]
# program = "zsh"

[window]
padding.x = 10
padding.y = 10
decorations = "None"
# opacity = 0.97

# [keyboard]
# bindings = [{ key = "F11", action = "ToggleFullscreen" }]
